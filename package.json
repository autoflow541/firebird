{
  "name": "firebird-show-control",
  "version": "0.2.0",
  "private": true,
  "description": "Auto-Flow Firebird — local-first live show control (Ableton clock, OSC, Blaize visuals) with a token-gated phone remote",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node test/engine.test.js",
    "check": "node --check src/main.js && node --check src/engine.js && node --check src/config.js && node --check src/renderer.js && node --check src/remote.js && node --check src/projector.js"
  },
  "dependencies": {
    "obs-websocket-js": "^5.0.6"
  },
  "devDependencies": {
    "electron": "^37.2.3"
  }
}
