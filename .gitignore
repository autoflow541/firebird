node_modules/
dist/
build/
.next/
.openai/
# Local operator config (may hold host/port specifics for a venue)
firebird.config.json
# Per-venue projection mapping + uploaded reference photos
firebird.mapping.json
mapping-photos/
# Saved screen captures
captures/
# OS / editor cruft
.DS_Store
Thumbs.db
*.log
